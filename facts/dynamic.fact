#!/usr/bin/bash

MAX=10
RESULT=0
FACT_NAME="personal_greetings"

for i in $(seq $MAX -1 1); do
  /usr/bin/test -e /etc/hello-${i}.txt
  if [ $? -eq 0 ]; then
     RESULT=$(cat /etc/hello-${i})
     break
  fi
done

echo "${FACT_NAME}=${RESULT}"
